import { defineMessages } from "react-intl";

const base = "components.searchHeader";

export default defineMessages({
    changeTheme: {
        id: `${base}.changeTheme`,
        defaultMessage: "Change Theme",
    },
    changeLang: {
        id: `${base}.changeLang`,
        defaultMessage: "Change Language",
    },
});
